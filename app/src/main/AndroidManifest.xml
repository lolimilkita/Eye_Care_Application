<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vira.eyecareapplication">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EyeCareApplication">
        <activity android:name=".quiz.HasilQuiz"></activity>
        <activity android:name=".quiz.Quiz2" />
        <activity android:name=".quiz.Quiz" />
        <activity android:name=".info.MataSehat" />
        <activity android:name=".info.GejalaMata" />
        <activity android:name=".info.MenjagaMata" />
        <activity android:name=".info.GangguanMata" />
        <activity android:name=".TentangApp" />
        <activity android:name=".MainActivity" />
        <activity android:name=".test.HasilTest" />
        <activity android:name=".test.TestKedua" />
        <activity android:name=".test.TestPertama" />
        <activity android:name=".anatomy.StrukturFungsi" />
        <activity android:name=".anatomy.Definisi" />
        <activity android:name=".anatomy.CaraKerja" />
        <activity android:name=".anatomy.EyeAnatomy" />
        <activity android:name=".DokterMataBekasi" />
        <activity android:name=".test.TestAmslerGrid" />
        <activity android:name=".fakta.FactEye" />
        <activity android:name=".info.InfoEyeCare" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".DisplayMapsActivity"
            android:label="@string/title_activity_display_maps" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>